<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.commonsystem.board.CommentMapper">

  <select id="findByPost" resultType="com.example.commonsystem.board.Comment">
    SELECT c.comment_id AS commentId,
           c.post_id AS postId,
           c.author_id AS authorId,
           u.name AS authorName,
           c.content,
           c.created_at AS createdAt
      FROM comments c
      JOIN users u ON u.user_id = c.author_id
     WHERE c.post_id = #{postId}
     ORDER BY c.comment_id ASC
  </select>

  <insert id="insert" parameterType="com.example.commonsystem.board.CommentCreateCommand">
    INSERT INTO comments(post_id, author_id, content)
    VALUES(#{postId}, #{authorId}, #{content})
  </insert>

</mapper>
